# This file is generated by Nx.
# Build the docker image with `npx nx docker:build @webnodex/gateway-service`.
# Tip: Modify "docker:build" options in project.json to change docker build args.
#
# Run the container with `nx docker:run @webnodex/gateway-service -p 3000:3000`.
#
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000


WORKDIR /app

COPY dist .

EXPOSE 3000

CMD ["node", "main.mjs"]
